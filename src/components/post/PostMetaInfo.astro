---
import { RelativeDate } from './RelativeDate'

interface Props {
  createAt: Date
  updateAt?: Date
  words: number
  readingMinutes: number
  class?: string
}

const { createAt, updateAt, words, readingMinutes, class: className } = Astro.props
---

<div class="flex flex-wrap gap-2 text-sm text-secondary" class:list={[className]}>
  <div>
    <i class="iconfont icon-calendar"></i>
    <RelativeDate date={createAt} client:idle />
    {updateAt && <span class="text-xs">(已编辑)</span>}
  </div>
  <div>
    <i class="iconfont icon-file-list"></i>
    <span>{words} 字</span>
  </div>
  <div>
    <i class="iconfont icon-timer"></i>
    <span>{Math.ceil(readingMinutes)} 分钟</span>
  </div>
</div>
