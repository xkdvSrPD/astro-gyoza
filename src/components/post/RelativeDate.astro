---
import { getFormattedDate, getRelativeTime } from '@/utils/date'

interface Props {
  date: Date
}

const { date } = Astro.props
const displayDate = getRelativeTime(date) || getFormattedDate(date)
---

<span>{displayDate}</span>
